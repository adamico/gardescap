- if user_signed_in? && current_user.admin?
  %li.dropdown
    = link_to "#", class: "dropdown-toggle", "data-toggle" => "dropdown" do
      Admin
      %b.caret
    %ul.dropdown-menu
      %li= link_to "Modifier le compte", edit_user_registration_path
      %li= link_to "Nouveau tableau de gardes", new_period_path
%li= link_to "Consulter le tableau actuel", tableau_actuel_path(Period.active.last)
%li= link_to "Remplir prochain tableau", choix_path
%li= link_to "Se déconnecter", destroy_user_session_path, method: :delete if user_signed_in?
